// Closes the modal on a soft-nav to the capture base /apps/[slug]/[date] (e.g. the
// screen lightbox's app-logo breadcrumb): the required [...catchAll] matches ≥1
// segment only, so the zero-segment base needs its own slot page resolving to null.
export default function ModalBase() {
  return null
}
